# 🚀 快速使用指南

由于 WSL 本地编译需要完整的 iOS 工具链（网络下载受限），**强烈建议使用 GitHub Actions 云端编译**。

## 📦 方法 1: GitHub Actions 云端编译（推荐）

### 步骤：

1. **创建 GitHub 仓库**
   - 访问 https://github.com/new
   - 仓库名随意（如 `FlashlightDaemon`）
   - 选 Public 或 Private
   - **不要**勾选任何选项

2. **推送代码到 GitHub**

   在当前目录（`C:\Users\Weeeiiii\led`）打开 **Git Bash** 或 **PowerShell**，执行：

   ```bash
   # 替换成你的 GitHub 用户名和仓库名
   git remote add origin https://github.com/你的用户名/FlashlightDaemon.git
   git branch -M main
   git push -u origin main
   ```

   如果提示输入凭据，使用你的 GitHub 用户名和 Personal Access Token。

3. **等待自动编译**
   
   - 推送后，访问仓库的 **Actions** 标签
   - 看到 "Build deb package" 工作流正在运行
   - 等待 2-3 分钟直到显示绿色勾 ✅

4. **下载编译好的 .deb**
   
   - 点击完成的工作流
   - 下载 **flashlight-daemon** artifact（压缩包）
   - 解压得到 `com.flashlight.daemon_1.0.0_iphoneos-arm.deb`

5. **安装到 iPhone**
   
   传到 iPhone 后：
   ```bash
   dpkg -i com.flashlight.daemon_*.deb
   cat /var/log/flashlightd.log  # 查看访问地址
   ```

---

## 📦 方法 2: 直接下载预编译包

如果你不想用 GitHub，我可以提供一个简化的指导：
1. 把项目文件打包发给有 Mac 的朋友
2. 或者使用在线 iOS 编译服务

---

## 🛠️ 为什么 WSL 编译失败？

iOS 交叉编译需要：
- Apple 官方的 LLVM/Clang 工具链（约 200MB+）
- 正确的链接器配置
- 网络能稳定访问 GitHub

你的 WSL 环境网络连接 GitHub 受限，无法下载完整工具链。

---

## ✅ 推荐操作

**现在就用 GitHub Actions 编译**：

1. 告诉我你的 GitHub 用户名
2. 我帮你生成推送命令
3. 你复制粘贴执行
4. 2 分钟后下载编译好的 .deb

或者你自己手动推送到 GitHub，工作流会自动编译。

---

**需要帮助？** 告诉我你的 GitHub 用户名，或者是否需要其他方案。
